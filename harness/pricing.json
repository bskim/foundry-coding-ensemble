{
  "_comment": "Per-1M-token USD list prices (gross, no cache discount), Azure AI Foundry Global Standard. 'gross' means every prompt token is billed at the input rate and every completion token at the output rate; the same method is applied to every config so $/resolved is directly comparable. Update these rates to match your tenant before relying on the cost numbers.",
  "unit": "per_1M_tokens",
  "currency": "USD",
  "models": {
    "gpt-5.4":         { "input": 2.50, "output": 15.00 },
    "kimi-k2":         { "input": 0.95, "output": 4.00 },
    "deepseek-v4-pro": { "input": 1.74, "output": 3.48 },
    "minimax-m2.5":    { "input": 0.30, "output": 1.20 },
    "grok-4.3":        { "input": 1.25, "output": 2.50 },
    "glm-5.1":         { "input": 1.54, "output": 4.84 },
    "deepseek-v3.1":   { "input": 1.74, "output": 3.48 }
  }
}
